<form method="post" action="{{ path('tutorial_back_delete', {'id': tutorial.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tutorial.id) }}">
    <button class="btn btn-danger" style="width: 100px">Delete</button>
</form>
